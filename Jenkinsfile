pipeline {

  agent any

  stages {
    
    stage('build') {
      steps {
        echo 'build'
        echo 'finish'
      }
    }

    stage('build docker image') {
      steps {
        echo 'build docker image'
      }
    }

  }
}